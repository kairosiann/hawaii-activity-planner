# Required: an externally reachable MySQL database containing the Drizzle schema.
DATABASE_URL=mysql://username:password@hostname:3306/database_name

# Optional: only needed if the legacy Manus OAuth flow is intentionally kept.
# OAUTH_SERVER_URL=
# VITE_APP_ID=
# VITE_OAUTH_PORTAL_URL=
# JWT_SECRET=
